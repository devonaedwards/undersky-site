# undersky.app

The public marketing and legal site for undersky.

**Source of truth is `_sites/undersky/` in the astro-workspace repo.** This repo is a
deploy copy: edit there, then republish. Editing here means the next deploy
overwrites you.

Static HTML, no build step. GitHub Pages serves it at the domain in `CNAME`.
